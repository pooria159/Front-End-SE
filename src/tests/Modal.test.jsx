// import { render, screen } from "@testing-library/react";
// import { defineTest } from 'vitest'
// import Modal from '../components/OfferPage/Modal'

// defineTest({
//   name: 'should render the modal component when isVisible prop is true',
//   run: async ({ is }) => {
//     // Arrange
//     const isVisible = true;
//     const onClose = () => {};
//     const isAccept = false;
//     const index = 0;
//     const removeCard = () => {};

//     // Act
//     render(<Modal isVisible={isVisible} onClose={onClose} isAccept={isAccept} index={index} removeCard={removeCard} />)

//     // Assert
//     is.truthy(screen.getByTestId('modal-component'))
//   }
// })
